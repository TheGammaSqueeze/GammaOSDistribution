<?xml version="1.0" encoding="utf-8"?>
<manifest android:sharedUserId="android.uid.system" package="com.android.keychain" xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-sdk android:minSdkVersion="32" android:targetSdkVersion="32"/>
    <!--
         Needed so KeyChainService on non-system user can write 
         security logging events
    -->
    <uses-permission android:name="android.permission.READ_LOGS"/>
    <uses-permission android:name="android.permission.HIDE_NON_SYSTEM_OVERLAY_WINDOWS"/>

    <application android:allowBackup="false" android:appComponentFactory="androidx.core.app.CoreComponentFactory" android:extractNativeLibs="true" android:label="@string/app_name" android:theme="@android:style/Theme.DeviceDefault.DayNight" android:usesCleartextTraffic="false" android:usesNonSdkApi="true">
        <service android:exported="true" android:name="com.android.keychain.KeyChainService">
            <intent-filter>
                <action android:name="android.security.IKeyChainService"/>
            </intent-filter>
        </service>

        <activity android:excludeFromRecents="true" android:exported="true" android:launchMode="singleTop" android:name="com.android.keychain.KeyChainActivity" android:theme="@style/KeyChainTransparent">
            <intent-filter>
                <action android:name="com.android.keychain.CHOOSER"/>

                <category android:name="android.intent.category.DEFAULT"/>
            </intent-filter>
        </activity>
    </application>

</manifest>
